import { DetalleBimestreCompetencia } from './detalle-bimestre-competencia';

describe('DetalleBimestreCompetencia', () => {
  it('should create an instance', () => {
    expect(new DetalleBimestreCompetencia()).toBeTruthy();
  });
});
