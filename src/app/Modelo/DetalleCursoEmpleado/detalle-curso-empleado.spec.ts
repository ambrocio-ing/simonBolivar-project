import { DetalleCursoEmpleado } from './detalle-curso-empleado';

describe('DetalleCursoEmpleado', () => {
  it('should create an instance', () => {
    expect(new DetalleCursoEmpleado()).toBeTruthy();
  });
});
